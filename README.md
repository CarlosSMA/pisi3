## Modo de uso

1. Ative o ambiente virtual do python via `python3 -m venv .venv` e `source .venv/bin/activate`, respectivamente.
2. Instale as dependências necessárias via comando `pip3 install -r requirements.txt`
3. Execute a aplicação Dash via `python main.py`
